import {AppBar, Container} from "@mui/material";

export default function NavBar() {
        return (
            <AppBar>
                <Container maxWidth={"xl"}>

                </Container>
            </AppBar>
        )
}